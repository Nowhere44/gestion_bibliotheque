{% extends 'base.html.twig' %}

{% block title %}Modifier ma bibliothèque{% endblock %}

{% block body %}
    <h1>Modifier ma bibliothèque</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Mettre à jour</button>
    {{ form_end(form) }}
{% endblock %}
